import { AuthProvider } from "../../providers/AuthProvider";
/**
 * Get the auth client singleton
 * @returns {AuthProvider} - The auth client singleton
 */
export declare const getAuthClient: () => AuthProvider;
